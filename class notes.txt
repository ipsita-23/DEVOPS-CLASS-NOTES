DOCKER

Use Docker for:
Backend deployment
Microservices
Running databases (MongoDB, MySQL)
ML model serving
CI/CD pipelines

Docker commands:
docker --version        //checks if docker is installed, if yes then which version
docker info             //shows detailed information about your Docker setup — basically a full status report of the Docker engine.
docker pull httpd       // to pull the image from dockerhub
docker history httpd    //shows the history of docker image with the image name mentioned in the command (httpd-image name)
docker history python   //(python-image name)
docker images           //list of all the images in our docker desktop along with their IDs, disk usage, content size 
docker run              //to create and start a new container, used with some executable commands
docker search nginx     //we can search in the dockerhub if a particular image exists, here-nginx.
docker run nginx        //running nginx container (creates a copy of nginx and uses it, lly like instance of a class)
docker start nginx      // only to create a container ,not start
docker start <containerID> //to start the existing container
docker start <container_name> //if we created the container with a name
docker run -d --name nginx nginx   //here --name gives the container a name nginx
docker stop nginx        //to stop the running container




docker --version
docker info
docker pull httpd       // to pull the image from dockerhub
docker history httpd    //shows the history of docker image with the image name mentioned in the command (httpd-image name)
docker history python   //(python-image name)
docker images           //list of all the images in our docker desktop along with their IDs, disk usage, content size 
docker run              // to create and start a new container, used with some executable commands

EXECUTABLE COMMANDS to be used with docker run :
	-it       : runs the container in interactive mode allowing u to interact with the container in the interactive mode
	-d        : runs the container in the background, detached mode
	--name    : specifies a name for the container
	--rm      : automatically removes the container when it exits
	-p        : maps a host port to a container port
	-e/ --env : sets an environment variable inside the container
	-v        : mounts a host volume inside the container
	examples: docker run -it httpd , docker run -d httpd
docker ps :shows all running containers
docker ps -a :shows all the containers
docker run httpd echo "Hello, World!"                             //randomly takes any container, gives name by itself
docker run --name my-container httpd echo "Hello, World!"   //running a container with a predefined name , here: my-container

// dockerfile is used to create a docker image that runs a shellscript automatically when the container starts

docker build -t myapp:v1 .
docker run -e MODE=production myapp:v1
docker run -e MY_VAR=value httpd env

docker run -it -e MY_NAME=Ipsita ubuntu bash
root@a7ba69f33284:/# echo $MY_NAME
Ipsita

docker run -d -e APP_ENV=production -e APP_VERSION=1.0 nginx
docker exec -it 3bad599d61b env 

docker run -d -e MYSQL_ROOT_PASSWORD=root123 -e MYSQL_DATABASE=college -e MYSQL_USER=admin -e MYSQL_PASSWORD=admin123 mysql:8 

-p stands for publish port -> it maps a port on your host machine to a port inside the container.
Without -p -> runs in an isolated environment
docker run -p <HOST PORT><CONTAINER PORT> IMAG
docker run -p 8080:80 nginx


Q. Run a docker container named "DB-app" based on the "mongodb" image and expose port 80 on the host to port 8082 on the container.

docker run -d  --name DB-app -p 80:8082 mongodb


Docker provides 3 types of data storage:
1. Volumes-
   is persistent, is managed by docker, used in databases, production
2. Bind Mounts-
   is persistent, not managed by docker, used for development
3. tmpfs-
   not persistent, managed by docker in memory, used for secrets, cache


Docker Volumes- Independent of container lifecycle, can be shared by multiple containers, managed fully by a docker

Without volumes-> data is stored inside the container, and on deleting the container, data is lost
With volumes-> data is stored on the host machine, containers can be recreated without losing data

docker volume create <volume name>
docker volume ls
docker volume inspect mydata
docker run -d -v <volumename>:<path where to store in image> --name <containername><imagename> 
eg:
docker volume create mydata
docker volume ls
docker volume inspect mydata
docker run -dit -v mydata:/app/data --name cont1 ubuntu  //-v: for volume , mydata:/app/data ->container address
docker exec -it cont1 bash
 echo "Hello Docker Volume">/app/data/file1.txt
 cat /app/data/file1.txt
 exit
docker rm -f cont1
docker run -it -v mydata:/app/data ubuntu bash
cat /app/data/file1.txt
exit
docker volume rm mydata 


Q. You need to start a new container using the nginx image while setting an environment variable ENV_MODE=Production. Write the docker run command to achieve this.

docker run -d --name my-new-container -e ENV_MODE=Production nginx 


Q. You need to start a new container using the nginx image while setting an env variable ENV_MODE=production. Write the docker run command to achieve this.

docker run -e ENV_MODE=production nginx

Q. You've a running container named my_app, but it is not behaving as expected. You want to check its logs to debug any errors. Write the commands to view its logs and follow new log entries in real time.

docker logs -f my_app

Q. A container named web_server was stopped. Write the command to: a) start the container again b) stop the container when needed.

a) docker start web_server
b) docker stop web_server 

Q1. Update the ubuntu container, install apache and php, then push updated on to docker hub.

   docker pull ubuntu
   docker run -it --name my_ubuntu ubuntu
   apt update
   apt install -y apache2 php libapache2-mod-php
   apache2 -v
   php -v
   exit
   docker commit my_ubuntu your_dockerhub_username/ubuntu-apache-php
   docker login
   docker push your_dockerhub_username/ubuntu-apache-php


Q2. Compare the size of base image and updated image after commit.
Q3. Creating a python flask web app( run a python web application inside docker so it works anywhere without setup issues). 

individual project- practical applications of the concepts learned regarding docker, maven,gihtub, actions and jenkins
GHCR

Q4. Imagine you're managing a project with multiple containers. Answer the following:
  a)How will you build an image and run a container? 
docker build -t my-app-image:v1 .
docker run -d --name my-running-app -p 8080:80 my-app-image:v1
  b)How do you save changes made to a running container? 
docker commit my-running-app my-new-custom-image:v2

  c)How can you go inside containers(for eg., a web app and a database)?
 for web app: docker exec -it web_app_container sh
 for database: docker exec -it db_container bash
  d)How to pull image from docker hub?
docker pull nginx:latest

Q5.You are assigned a basic DevOps task to deploy a static web page with Docker. Your objective is to pull the latest NGINX docker image, create a simple HTML page, run the container and display the HTML page using NGINX static hosting. Use docker volume mounting to connect your HTML file to the container.

Q6. You are required to create a customized MySQL Docker image with a preloaded database and publish it to GitHub Container Registry (GHCR) then pull and verify it on another system.

Q. Design an docker compose configuration using environment variables to configure communication between a Node.js backend service and a MongoDB database service.

Q. A company wants to build a high performance backend service using  C++ that communicates with a MongoDB database. Design a docker compose setup to deploy both services using env variables.
Q. Compare implementing a microservice using using Node.js and C++ in docker compose. Discuss performance complexity and scalability. 


WORDPRESS + MySQL


Q. Using docker compose, create a docker-compose.yml configuration to deploy a microservices based application consisting of 3 services- frontend, backend and database. The yaml file should define version, services, networks, volumes, include environment variables. Additionally, specify when to use build vs image fields in the yaml file and configure sevice dependency ordering so that backend services start only after the database. 
Q. Pull the nginx image from the public repository on docker hub and create a container from it, run the container in detached mode, and display the list of running containers with appropriate commands.